{
  "name": "serverless-efs-lambda",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "deploy": "sls deploy --aws-profile=schueler --stage=dev"
  },
  "dependencies": {
    "serverless-pseudo-parameters": "^2.5.0"
  }
}
